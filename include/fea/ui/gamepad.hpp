#pragma once

namespace fea
{
    namespace Gamepad
    {
        enum Axis
        {
            X,
            Y,
            Z,
            R,
            U,
            V,
            PovX,
            PovY
        };
    }
    /** @addtogroup UI
     *@{
     *  @enum Gamepad::Axis
     *@}
     ***
     *  @enum Gamepad::Axis
     *  @brief Numerical value representing an axis on a gamepad device.
     ***/
}
